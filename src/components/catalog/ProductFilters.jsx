import React from "react";
import { bem } from "../../lib/bem";

export default function ProductFilters() {
  return (
    <div className={`${bem("catalog", "filters")}`}>
      <a>FiltersFiltersFiltersFilters</a>
    </div>
  );
}
